# HTML + CSS + Docker Sample

This project is a simple visual landing page built with HTML and CSS, served via Docker using NGINX.

## Run with Docker

```bash
docker build -t html-css-demo .
docker run --rm -p 8080:80 html-css-demo
```

Open http://localhost:8080 in your browser.
